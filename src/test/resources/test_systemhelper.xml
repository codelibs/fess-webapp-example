<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE components PUBLIC "-//DBFLUTE//DTD LastaDi 1.0//EN"
	"http://dbflute.org/meta/lastadi10.dtd">
<components>
	<include path="convention.xml" />
	<include path="lastaflute.xml" />
	<!-- Load the plugin's OVERRIDE definition so the test exercises the same
		redefine wiring that Fess applies at runtime: this replaces the core
		"systemHelper" component with CustomSystemHelper. -->
	<include path="fess+systemHelper.xml" />
</components>
